# 多智能体任务博弈的动态攻防网络与鲁棒性对抗实验报告

---

## 一、实验背景

随着大型社交网络（如 Facebook、微博、微信）规模的急剧扩大，其底层网络拓扑结构的稳定性与抗毁性已成为网络科学研究的热点问题。在真实场景中，网络不仅面临随机故障，更面临来自攻击者的**针对性破坏**——攻击者通过识别网络中的关键节点和链路，实施精确打击以最大化破坏效果。与此同时，网络管理者需要通过实时修复和拓扑加固来维持网络的连通性。

传统的网络攻防研究多采用固定策略（如随机攻击、度数攻击），这些方法无法体现攻击与防御双方在博弈过程中的**自适应学习能力**。近年来，大语言模型（LLM）的快速发展为构建智能化、自适应攻防策略提供了新的可能。

本项目基于 **AgentScope 多智能体框架**，设计并实现了一个多智能体攻防博弈实验平台，研究 AI 驱动的自适应网络攻击与防御策略。平台模拟 Facebook 社交网络分析流水线，包含 8 个任务节点、攻击智能体（代号"黑翼"）和防御智能体（代号"铁壁"），支持规则基策略与 LLM 自适应策略的对照实验，为网络鲁棒性研究提供了统一的实验基准。

---

## 二、实验要求

本实验要求设计并实现一个完整的多智能体攻防博弈系统，具体要求如下：

1. **多智能体系统构建**：基于 AgentScope 框架，构建包含攻击智能体、防御智能体和 8 个任务节点智能体的多智能体系统。
2. **网络攻防博弈机制**：攻击方可破坏网络链路，防御方可修复被攻击链路并主动新增防护链路，双方在有限资源约束下进行多轮博弈。
3. **自适应策略设计**：智能体需具备记忆机制，能够基于历史攻防经验调整策略。可选接入大模型（LLM）实现自适应决策。
4. **网络鲁棒性评估**：建立完整的指标体系，从连通性、效率、结构完整性等多维度量化评估网络抗毁性。
5. **对照实验**：设计多组对照实验，对比随机攻击、度数攻击、LLM 自适应攻击在不同网络结构上的效果差异。
6. **可视化与报告**：实时可视化博弈过程，记录攻防日志，生成指标变化图表和实验报告。

---

## 三、实验环境

### 3.1 硬件环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 10/11 (PowerShell) |
| 处理器 | 多核 CPU |
| 内存 | 8GB+ RAM |
| 存储 | 硬盘空间用于数据、日志存储 |

### 3.2 软件环境

| 项目 | 版本/说明 |
|------|----------|
| Python | 3.8+ |
| NetworkX | 网络拓扑构建与分析 |
| Flask | 后端 REST API 服务 |
| Vue.js | 前端图形化界面 |
| Ollama（可选） | 本地大模型推理服务 |
| Matplotlib | 指标可视化 |
| dotenv | 环境变量管理 |

### 3.3 依赖框架与库

| 库名 | 用途 |
|------|------|
| `networkx` | 图构建与网络分析 |
| `numpy` | 数值计算 |
| `flask` + `flask-cors` | 后端 API |
| `matplotlib` | 图表绘制 |
| `python-dotenv` | 环境配置 |
| `requests` | HTTP 请求（模型 API 调用） |
| `ollama` | 本地 LLM 推理 |

---

## 四、实验数据

### 4.1 Facebook 社交网络数据集

实验使用 **Stanford SNAP 项目**提供的 Facebook 社交网络数据集（`facebook_combined.txt`），该数据集特征如下：

| 属性 | 数值 |
|------|------|
| 节点数（用户） | 4,039 |
| 边数（好友关系） | 88,234 |
| 平均度 | ≈ 43.7 |
| 数据来源 | SNAP: https://snap.stanford.edu/data/facebook_combined.txt |

### 4.2 合成网络

除真实数据集外，平台还支持多种合成网络用于对照实验：

| 网络类型 | 生成算法 | 特点 |
|----------|----------|------|
| **Watts-Strogatz 小世界网络**（默认） | `nx.watts_strogatz_graph(n, k, p)` | 高聚类系数、短平均路径 |
| **Barabasi-Albert 无标度网络** | `nx.barabasi_albert_graph(n, m)` | 度分布符合幂律，存在 hub 节点 |
| **完全图** | `nx.complete_graph(n)` | 每对节点相连，冗余度最高 |
| **环形网络** | `nx.cycle_graph(n)` | 每个节点仅与邻居相连，最脆弱 |

### 4.3 默认实验网络

默认配置使用 **Watts-Strogatz 小世界网络**，参数设置如下：

```python
# core/network_manager.py
nx.watts_strogatz_graph(n=8, k=3, p=0.1, seed=42)
# 8节点，平均度=3，重连概率=0.1
```

初始网络：8 个节点，8 条边（平均度 2.0），保证连通性。

---

## 五、实验设计

### 5.1 智能体设计

#### 5.1.1 智能体基类（BaseGameAgent）

所有智能体继承自 `BaseGameAgent`（`agents/base_agent.py`），该基类定义了智能体的通用架构：

```python
class BaseGameAgent(ABC):
    def __init__(self, name, role_description, model_provider,
                 temperature, max_tokens, use_llm):
        self.name = name              # 智能体名称
        self.role_description = role_description  # 角色描述（用于prompt）
        self.memory = AgentMemory()    # 记忆系统
        self.system_prompt = self._build_system_prompt()  # 系统提示
        self.use_llm = use_llm        # 是否启用LLM
```

核心能力：
- **记忆系统**（`AgentMemory`）：支持添加、查询、按类型检索记忆，用于智能体积累攻防经验
- **LLM 推理接口**（`think()`）：通过 `model_manager.chat()` 调用 Ollama 或在线 API
- **双模决策**：优先使用 LLM，自适应决策；LLM 不可用时回退到规则基策略

#### 5.1.2 攻击智能体（AttackerAgent）

代号"黑翼"，继承自 `BaseGameAgent`，负责分析网络结构并选择攻击目标。

**角色描述**（系统提示词）：

```
你是网络攻击智能体，代号"黑翼"。
你的目标是在有限的攻击次数内，最大化破坏网络的连通性和任务执行能力。
能力限制：每轮最多攻击2条链路，每次攻击必须选择当前网络中存在的链路。
攻击策略：
1. 分析网络结构，识别关键节点和链路
2. 优先攻击那些被修复过的链路（防御方的弱点）
3. 优先攻击核心节点之间的连接
4. 记录每次攻击的效果，用于后续策略优化
```

**决策流程**（`decide_attack_targets()`）：

1. 从网络状态获取活跃链路列表
2. 获取关键链路（基于介数中心性，`get_critical_edges()`）
3. 检查近期被防御方修复过的链路（优先攻击的弱点）
4. 若启用 LLM，将网络状态、历史记忆打包为上下文，调用 `think()` 获取 LLM 决策
5. 去重、限制数量，不足时随机补充
6. 返回攻击目标列表

**执行模块**（`execute_attack()`）：调用 `network_manager.remove_edge()` 移除链路，记录攻击历史到记忆系统。

#### 5.1.3 防御智能体（DefenderAgent）

代号"铁壁"，继承自 `BaseGameAgent`，负责修复被攻击链路和新增防护链路。

**角色描述**：

```
你是网络防御智能体，代号"铁壁"。
你的目标是维护网络的连通性和任务执行能力，在有限资源下最大化防御效果。
能力限制：每轮最多修复2条被破坏的链路，每轮最多新增1条防护链路。
防御策略：
1. 优先修复被频繁攻击的关键链路
2. 优先保护核心节点，添加旁路连接
3. 优化网络拓扑，提高整体抗毁性
4. 记录每次防御的效果，用于后续优化
```

**决策流程**（`decide_defense_actions()`）：

1. 遍历初始链路列表，将不在当前活跃边集中的边标记为待修复（最多 2 条）
2. 调用 `_find_new_edge_candidates()` 寻找可新增链路候选（优先为关键节点添加旁路）
3. 若启用 LLM，补充决策优化
4. 去重、限制数量，返回修复列表和新增列表

**执行模块**（`execute_defense()`）：调用 `network_manager.add_edge()` 添加/修复链路。

#### 5.1.4 任务节点智能体（TaskNodeAgent）

8 个任务节点智能体构成 Facebook 社交网络分析流水线，每个节点有独立角色和上下游依赖关系：

| 节点 ID | 节点名称 | 上游节点 | 下游节点 | 处理能力 |
|---------|----------|----------|----------|----------|
| Node-1 | 数据采集节点 | — | Node-2, Node-4 | 1000 |
| Node-2 | 关系解析节点 | Node-1 | Node-3 | 800 |
| Node-3 | 社区发现节点 | Node-2 | Node-4, Node-6, Node-7 | 600 |
| Node-4 | 中心性分析节点 | Node-1, Node-3 | Node-5, Node-8 | 700 |
| Node-5 | 传播模型节点 | Node-4 | Node-8 | 500 |
| Node-6 | 异常检测节点 | Node-3 | Node-7, Node-8 | 400 |
| Node-7 | 聚类分析节点 | Node-3, Node-6 | Node-8 | 500 |
| Node-8 | 结果汇总节点 | Node-4, Node-5, Node-6, Node-7 | — | 1000 |

节点配置定义于 `config/network_config.py` 的 `NODE_ROLES` 列表中，工厂函数 `create_all_task_nodes()` 实例化所有节点。

### 5.2 网络设计

#### 5.2.1 网络管理器（NetworkManager）

核心类 `NetworkManager`（`core/network_manager.py`）负责网络的构建与状态维护：

- **网络初始化**：支持 4 种网络类型生成（WS 小世界、BA 无标度、完全图、环形），确保初始网络连通
- **链路操作**：`remove_edge(u, v)` 删除链路，`add_edge(u, v)` 添加链路，节点 ID 统一处理字符串/整数转换
- **关键节点/链路分析**：`get_critical_nodes()` 结合度中心性和介数中心性综合评分；`get_critical_edges()` 基于介数中心性
- **网络状态**：`get_network_info()` 返回节点数、边数、连通分量数等；`is_network_paralyzed(threshold)` 判断网络是否瘫痪

#### 5.2.2 网络拓扑数据流

```
初始网络（8节点, 8边）
    ↓
GameEngine._run_single_round()
    ├── 攻击阶段：AttackerAgent → NetworkManager.remove_edge()
    ├── 状态检测：NetworkManager.is_network_paralyzed()
    ├── 防御阶段：DefenderAgent → NetworkManager.add_edge()
    └── 状态记录：RobustnessMetrics.calculate()
```

### 5.3 攻防策略设计

#### 5.3.1 对照组策略

**对照组 1：随机攻击（RandomAttacker）**

```python
# strategies/random_attack.py
class RandomAttacker:
    def select_targets(self, network_state, **kwargs):
        edges = network_state.get("edges", [])
        num_to_attack = min(self.max_attacks, len(edges))
        return random.sample(edges, num_to_attack)
```

特点：完全随机选择攻击目标，无任何先验知识，无学习能力。

**对照组 2：度数攻击（DegreeAttacker）**

```python
# strategies/degree_attack.py
class DegreeAttacker:
    def select_targets(self, network_state, **kwargs):
        degree_dict = dict(G.degree())
        sorted_nodes = sorted(degree_dict.keys(), key=lambda x: degree_dict[x], reverse=True)
        # 优先选择度数最高节点相连的边
        for node in sorted_nodes:
            for neighbor in G.neighbors(node):
                edge = tuple(sorted([node, neighbor]))
                targets.append(edge)
```

特点：策略固定，优先攻击高度数节点相连的边，模拟传统蓄意攻击。

**对照组 3：基础防御（BaselineDefender）**

```python
# strategies/baseline_defense.py
class BaselineDefender:
    def select_actions(self, network_state, initial_edges, **kwargs):
        # 1. 修复初始链路（优先）
        for edge in initial_edges:
            if sorted_edge not in active_edges and len(actions["repairs"]) < self.max_repairs:
                actions["repairs"].append(sorted_edge)
        # 2. 随机新增链路
        for _ in range(new_edge_slots):
            u, v = random.sample(nodes, 2)
            actions["new_edges"].append(edge)
```

特点：规则固定，优先修复初始边，随机新增链路。

#### 5.3.2 LLM 自适应策略

**LLM 自适应攻击**：`AttackerAgent(use_llm=True)` 调用 `model_manager.chat()`，向 LLM 提供完整上下文（网络状态、关键链路、历史记忆），LLM 解析 JSON 决策输出后执行。

**LLM 自适应防御**：`DefenderAgent(use_llm=True)` 类似地向 LLM 提供攻防历史和当前网络状态，LLM 综合判断修复优先级和新增位置。

### 5.4 代码设计

#### 5.4.1 项目架构

```
Agent_attack_and_defense/
├── core/                        # 核心引擎
│   ├── game_engine.py            # 博弈引擎（主循环）
│   ├── network_manager.py        # 网络管理器
│   └── model_manager.py          # 模型管理器（Ollama/API统一调度）
├── agents/                       # 智能体
│   ├── base_agent.py             # 智能体基类
│   ├── attack_agent.py           # 攻击智能体（黑翼）
│   ├── defense_agent.py          # 防御智能体（铁壁）
│   └── task_node_agent.py        # 8个任务节点
├── strategies/                   # 对照实验策略
│   ├── random_attack.py          # 随机攻击
│   ├── degree_attack.py          # 度数攻击
│   └── baseline_defense.py       # 基础防御
├── metrics/                      # 指标计算
│   ├── robustness.py            # 网络鲁棒性指标
│   └── visualizer.py             # 图表可视化
├── config/                       # 配置
│   ├── game_config.py            # 博弈规则配置
│   ├── network_config.py         # 网络参数配置
│   └── model_config.py           # 模型配置（Ollama/DashScope/DeepSeek/文心）
├── backend/
│   └── app.py                    # Flask API + SSE 实时流
├── frontend/                     # Vue.js Web 界面
├── scripts/
│   └── load_facebook_data.py     # Facebook 数据集加载
├── main.py                       # 命令行主程序
├── run_experiments.py             # 对照实验脚本
└── requirements.txt
```

#### 5.4.2 核心模块交互

**博弈引擎主循环**（`GameEngine.run()`）：

```python
# core/game_engine.py
def run(self):
    self._record_round(is_initial=True)          # 记录初始状态
    while not self.game_over and self.current_round < self.max_rounds:
        self.current_round += 1
        self._run_single_round()                  # 执行单轮
        self._check_termination()                 # 检查终止条件
    return self._generate_summary()               # 生成结果
```

单轮执行流程（`_run_single_round()`）：

```python
# 1. 攻击阶段
attack_targets = self.attacker.decide_attack_targets(network_state, use_llm=self.use_llm_attacker)
attack_result = self.attacker.execute_attack(attack_targets, self.network)

# 2. 状态检测
is_paralyzed = self.network.is_network_paralyzed(self.paralysis_threshold)

# 3. 防御阶段
defense_actions = self.defender.decide_defense_actions(
    network_state_after_attack,  # 必须在攻击后重新获取
    self.initial_edges,
    use_llm=self.use_llm_defender
)
defense_result = self.defender.execute_defense(defense_actions, self.network)

# 4. 记录状态
self._record_round(attack_targets=..., repair_targets=..., new_edges=...)

# 5. 可视化更新
self.visualizer.update(self.network, self.current_round, ...)
```

**终止条件**（`_check_termination()`）：

- **攻击方胜利**：连续 3 轮网络最大连通分量占比 < 30%（可配置）
- **防御方胜利**：达到最大博弈轮次（默认 30 轮）且网络未瘫痪

#### 5.4.3 模型管理器

`ModelManager`（`core/model_manager.py`）统一调度多种模型后端：

```python
class ModelManager:
    def chat(self, messages, provider=None, **kwargs):
        if provider:
            client = self.clients.get(provider)
            if client and client.is_available():
                return client.chat(messages, **kwargs)
        # 自动选择优先级：Ollama > DashScope > DeepSeek > 文心一言
```

支持的模型提供商：
- **Ollama 本地**（默认，`qwen2.5:7b`）：无需网络，低延迟
- **阿里通义千问**（DashScope，`qwen-plus`）
- **DeepSeek**（`deepseek-chat`）
- **百度文心一言**（`ernie-4.0-8k-latest`）

#### 5.4.4 Flask API 服务

`backend/app.py` 提供完整的 REST API 和 SSE 实时流：

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/experiments` | POST | 创建新实验 |
| `/api/experiments` | GET | 获取所有实验列表 |
| `/api/experiments/<id>` | GET | 获取实验详情 |
| `/api/experiments/<id>/start` | POST | 启动博弈 |
| `/api/experiments/<id>/pause` | POST | 暂停博弈 |
| `/api/experiments/<id>/resume` | POST | 继续博弈 |
| `/api/experiments/<id>/stop` | POST | 停止博弈 |
| `/api/experiments/<id>/state` | GET | 获取当前博弈状态（含网络拓扑、智能体状态、指标） |
| `/api/experiments/<id>/metrics` | GET | 获取指标历史 |
| `/api/experiments/<id>/stream` | GET | SSE 实时流（EventSource） |
| `/api/experiments/<id>/analysis` | GET | 获取分析报告 |

博弈在独立线程（`threading.Thread`）中运行，通过 `stop_event` 和 `pause_event` 控制启停。SSE 流每 200ms 检查状态变化并推送完整状态数据。

### 5.5 实验评价指标设计

#### 5.5.1 核心指标体系

指标计算模块 `RobustnessMetrics`（`metrics/robustness.py`）提供完整指标体系：

| 指标名称 | 计算方法 | 物理含义 |
|----------|----------|----------|
| **最大连通分量比例** | `largest_cc_size / total_nodes` | 网络分裂程度 |
| **平均度数** | `sum(degree) / n` | 网络稀疏程度 |
| **聚类系数** | `nx.average_clustering(G)` | 局部聚团程度 |
| **平均最短路径** | `nx.average_shortest_path_length(G)` | 节点间通信效率 |
| **网络直径** | `nx.diameter(G)` | 最长最短路径 |
| **度中心化** | $\frac{\sum(\max d - d_i)}{\text{theoretical\_max}}$ | 度分布集中程度 |
| **介数中心化** | $\frac{\sum(\max b - b_i)}{\text{theoretical\_max}}$ | 关键节点重要性 |
| **网络效率** | `nx.global_efficiency(G)` | 全局通信效率 |
| **综合鲁棒性指数** | $0.5 \times cc_{ratio} + 0.3 \times efficiency + 0.2 \times clustering$ | 综合抗毁性评分 |

#### 5.5.2 博弈胜负判定

| 判定条件 | 胜负方 |
|----------|--------|
| 连续 3 轮 `largest_cc_ratio < 0.3` | **Attacker（攻击方胜利）** |
| 达到最大轮次且 `largest_cc_ratio >= 0.3` | **Defender（防御方胜利）** |

#### 5.5.3 对照实验指标

通过 `run_experiments.py` 的 `ExperimentRunner` 运行多组对照实验，评估不同策略组合的效果：

```python
# run_experiments.py
experiments = [
    ("随机攻击 vs 基础防御", RandomAttacker, BaselineDefender),
    ("度数攻击 vs 基础防御", DegreeAttacker, BaselineDefender),
    ("LLM自适应攻击 vs LLM自适应防御", AttackerAgent(use_llm=True), DefenderAgent(use_llm=True))
]
```

---

## 六、实验结果与分析

### 6.1 基础实验结果

以下为默认配置（Watts-Strogatz 网络，8 节点，3 轮博弈，不使用 LLM）下的实际运行结果：

#### 6.1.1 攻防博弈过程

| 轮次 | 攻击目标 | 修复链路 | 新增链路 | 当前边数 | 最大连通分量 |
|------|----------|----------|----------|----------|--------------|
| 0（初始） | — | — | — | 8 | 8 |
| 1 | (0,7), (4,5) | (4,5), (0,7) | (0,6) | 9 | 8 |
| 2 | (0,1), (0,6) | (0,1) | (1,3) | 9 | 8 |
| 3 | (6,7), (0,7) | (6,7), (0,7) | (3,5) | 10 | 8 |

**胜负判定**：达到最大轮次（3 轮），网络未瘫痪 → **Defender 胜利**

#### 6.1.2 指标变化曲线

| 轮次 | 链路数 | 最大连通分量比例 | 平均最短路径 | 聚类系数 | 鲁棒性指数 |
|------|--------|------------------|--------------|----------|------------|
| 0 | 8.0 | 1.0 | 2.286 | 0.000 | 0.669 |
| 1 | 9.0 | 1.0 | 2.179 | 0.208 | 0.720 |
| 2 | 9.0 | 1.0 | 2.071 | 0.125 | 0.705 |
| 3 | 10.0 | 1.0 | 1.929 | 0.208 | 0.732 |

**关键发现**：

1. **链路数净增长**：尽管每轮攻击 2 条边，防御方通过修复 + 新增策略，实现了链路数从 8 到 10 的净增长
2. **鲁棒性指数上升**：从初始 0.669 提升到 0.732（+9.4%），网络整体抗毁性增强
3. **平均路径缩短**：从 2.286 缩短到 1.929（-15.6%），新增链路改善了拓扑结构
4. **连通性维持**：所有轮次最大连通分量比例均为 1.0，网络始终保持完全连通

#### 6.1.3 更长轮次实验（5 轮）

在 5 轮博弈中，攻击方和防御方的拉锯更加明显：

| 轮次 | 攻击目标 | 修复 | 新增 | 最终边数 | 鲁棒性指数 |
|------|----------|------|------|----------|------------|
| 1 | (0,7), (4,5) | (0,7), (4,5) | (0,6) | 9 | 0.720 |
| 2 | (0,1), (0,6) | (0,1) | (1,3) | 9 | 0.705 |
| 3 | (6,7), (0,7) | (6,7), (0,7) | (3,5) | 10 | 0.732 |
| 4 | (5,6), (3,5) | (5,6) | (3,7) | 10 | 0.715 |
| 5 | (6,7), (2,3) | (2,3), (6,7) | (0,6) | 11 | 0.749 |

**观察**：随着博弈进行，网络拓扑从初始的环形链结构逐步演化为更密集的网状结构（节点 3 成为新的连接中枢），体现了防御方的"自适应加固"策略效果。

### 6.2 对照实验设计

#### 6.2.1 实验组设置

| 实验组 | 攻击策略 | 防御策略 | LLM 使用 | 预期 |
|--------|----------|----------|----------|------|
| **对照组 1** | 随机攻击（RandomAttacker） | 基础防御（BaselineDefender） | 否 | 防御方占优 |
| **对照组 2** | 度数攻击（DegreeAttacker） | 基础防御（BaselineDefender） | 否 | 攻击方更具威胁 |
| **实验组** | LLM 自适应攻击 | LLM 自适应防御 | 是 | 双方均有学习能力 |

#### 6.2.2 网络类型变量

| 网络类型 | 特性 | 预期攻击效果 |
|----------|------|--------------|
| Watts-Strogatz（WS） | 高聚类、短路径 | 中等脆弱性 |
| Barabasi-Albert（BA） | 幂律度分布 | hub 节点是关键，攻击更有效 |
| 完全图 | 全连通 | 极度冗余，难以攻击 |
| 环形网络 | 仅邻居连接 | 极度脆弱，单点攻击即分裂 |

#### 6.2.3 博弈参数变量

| 参数 | 默认值 | 变化范围 | 影响 |
|------|--------|----------|------|
| 每轮攻击次数 | 2 | 1~5 | 影响攻防节奏 |
| 每轮修复次数 | 2 | 1~5 | 影响防御能力 |
| 每轮新增链路数 | 1 | 0~3 | 影响拓扑优化能力 |
| 博弈最大轮次 | 30 | 10~100 | 影响实验时长 |
| 瘫痪阈值 | 0.3 | 0.1~0.5 | 影响胜负判定标准 |

### 6.3 结果分析

#### 6.3.1 攻防博弈博弈均衡分析

从实验数据来看，在默认配置下**防御方具有明显优势**：

1. **资源不对称**：防御方每轮可操作 3 次（2 修复 + 1 新增），而攻击方仅 2 次。长期来看，防御方的网络建设速度超过破坏速度。
2. **信息不对称**：防御方通过记忆系统能追踪被攻击链路，而攻击方的"再攻击"策略需要 LLM 才能有效实现。
3. **拓扑正反馈**：新增链路会改变网络拓扑，使后续攻击目标选择更加分散，降低了针对性攻击的效率。

#### 6.3.2 策略有效性对比

| 策略 | 优势 | 劣势 | 适用场景 |
|------|------|------|----------|
| 随机攻击 | 无需先验知识 | 效率低，容易攻击低价值目标 | 对照基线 |
| 度数攻击 | 针对 hub 节点，效果集中 | 无视局部结构，可能被绕过 | 无标度网络 |
| LLM 自适应攻击 | 综合分析，动态调整 | 依赖模型质量，响应慢 | 复杂网络、多轮博弈 |
| 基础防御 | 稳定可靠 | 无法应对针对性攻击 | 对照基线 |
| LLM 自适应防御 | 主动优化拓扑 | 资源消耗大 | 高价值网络保护 |

#### 6.3.3 指标相关性分析

从实验数据中可观察到以下相关性：

- **链路数 ↔ 鲁棒性指数**：正相关，但并非线性关系。如第 2 轮（9 边，鲁棒性 0.705）反而低于第 1 轮（9 边，鲁棒性 0.720），因为新增链路（0,6）改变了拓扑结构
- **平均路径 ↔ 网络效率**：强负相关，平均路径越短，效率越高
- **聚类系数 ↔ 抗毁性**：弱正相关，高聚类结构能提供更多冗余路径

---

## 七、实验总结与思考

### 7.1 实验总结

本项目成功实现了一个基于多智能体的网络攻防博弈实验平台，主要贡献如下：

1. **完整的多智能体系统**：攻击智能体"黑翼"、防御智能体"铁壁"和 8 个任务节点智能体构成完整的博弈体系，支持记忆机制和双模决策（LLM / 规则基）。

2. **丰富的攻防策略**：提供了 3 种攻击策略（随机、度数、LLM 自适应）和 2 种防御策略（基础、LLM 自适应），支持灵活组合和对照实验。

3. **完整的指标体系**：从连通性、效率、结构三个维度量化评估网络鲁棒性，综合鲁棒性指数 $R = 0.5 \times cc_{ratio} + 0.3 \times efficiency + 0.2 \times clustering$ 能够全面反映网络抗毁性。

4. **图形化实验平台**：基于 Flask + Vue.js 的 Web 界面支持实时可视化博弈过程，SSE 推送确保前端及时刷新；命令行工具支持批量对照实验。

5. **真实数据集支持**：集成 Stanford SNAP 的 Facebook 社交网络数据集（4039 节点，88234 边），支持从真实网络结构进行攻防博弈。

### 7.2 关键发现

1. **防御策略的资源优势**：在每轮 2 攻击 + 2 修复 + 1 新增的配置下，防御方能实现链路净增长，网络鲁棒性指数在博弈过程中呈上升趋势。

2. **拓扑演化的双刃剑效应**：防御方新增的链路在修复网络的同时，也改变了拓扑结构，可能为后续攻击提供新的"弱点"路径。

3. **LLM 自适应策略的潜力**：基于大模型的攻防智能体能综合分析网络状态和历史记忆，做出更合理的决策，但实际效果依赖于模型推理质量和响应速度。

### 7.3 局限性与改进方向

1. **节点失效机制缺失**：当前版本仅支持链路攻击/修复，未实现节点被攻击后失效（导致下游节点无法通信）的机制，与真实网络攻击场景存在差距。

2. **博弈深度不足**：默认 3 轮博弈难以充分体现自适应学习效果，建议将默认轮次提升至 30 轮以观察长期攻防演化。

3. **LLM 推理开销**：在 Web 界面中，LLM 推理的延迟（通常 1-5 秒）会影响实时博弈体验，建议增加"思考中"状态提示。

4. **实验可复现性**：应增加多次独立实验（10+ 次）取平均值的功能，以消除随机性影响并计算置信区间。

5. **更多攻防策略**：可引入基于介数中心性的攻击策略、基于网络流优化的防御策略等，丰富策略库。

### 7.4 未来展望

1. **动态节点能力**：引入节点处理能力的动态变化，模拟真实社交网络中用户活跃度的时变性。

2. **多阶段博弈**：设计多阶段攻防博弈，攻击方可选择不同攻击强度（如轻度干扰 vs 完全摧毁），防御方对应不同响应级别。

3. **分布式部署**：将博弈引擎部署到多台机器，实现真正的多智能体分布式协同。

4. **强化学习集成**：将 LLM 策略与强化学习结合，让攻防智能体通过与环境交互自主学习最优策略。

5. **跨网络迁移**：研究在某一网络拓扑上学到的攻防策略能否有效迁移到其他网络，提升方法的泛化能力。
