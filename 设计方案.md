题目：多智能体任务博弈的动态攻防网络与鲁棒性对抗

背景

传统网络鲁棒性研究往往假设节点失效是随机的（如随机攻击）或基于度数（蓄意攻击）。但在AI时代，攻击者本身也可以是一个智能体，它会学习、欺骗、自适应地切断关键链路。这种智能体对智能体的攻防博弈，将彻底改写网络鲁棒性的定义。

将网络鲁棒性从静态统计分析升级为动态AI对抗实验，可以让攻防智能体互相学习，上演一场网络版的“猫鼠游戏”。



实验设计方案

多智能体任务博弈的动态攻防网络与鲁棒性对抗

整体分为工具选型、实验整体架构、分步实现流程、指标计算、结果分析、演示与报告六大模块，全程零基础可跟着操作，选用开源免费、文档丰富的平台 / 工具，避开复杂底层开发。

一、首选工具与平台（新手优先，免费易上手）

（一）多智能体运行平台（二选一，推荐第一个）

首选：AgentScope（阿里开源多智能体框架）

优势：中文文档、开箱即用、支持智能体交互 / 决策 / 记忆 / 博弈，兼容本地大模型 / 开源模型，无需复杂部署，纯 Python 开发，适配学生作业。

适用：快速搭建防御智能体、攻击智能体、任务网络节点智能体三类角色。

备选：CrewAI

优势：轻量化、角色分工清晰，适合简单博弈场景，代码量极少；缺点：复杂网络拓扑支持弱，仅适合入门演示。

（二）网络建模与可视化工具

NetworkX（Python 库）：构建网络拓扑、计算鲁棒性指标、模拟链路 / 节点失效，复杂网络领域标准工具，零基础入门简单。

Matplotlib/Plotly：网络动态可视化，直观展示攻防过程中网络变化。

（三）大模型底座（智能体决策核心，3 种方案任选）

本地开源模型（完全免费，推荐新手）：Qwen、Llama2 7B 量化版，搭配 ollama 一键部署，本地运行不依赖外网。

在线 API（零部署，最快上手）：通义千问、文心一言、DeepSeek 免费学生 API，直接调用，省去模型部署麻烦。

极简模式（纯规则智能体，不用大模型）：如果不想对接大模型，用固定规则 + 简单决策逻辑写智能体，纯 Python 实现，门槛最低。

（四）运行环境

Python 3.8\~3.10（通用稳定版本），Anaconda 管理环境，全程代码基于 Python。

二、实验整体思路 \& 角色定义（先搞懂逻辑）

1\. 实验核心目标

搭建任务协作网络，设置防御方智能体和攻击方智能体，双方动态博弈：攻击方自适应破坏网络链路 / 节点，防御方实时修复、加固网络；全程观测网络鲁棒性变化，对比传统攻击与AI 智能体自适应攻击的差异。

2\. 三大角色（必设）

任务节点智能体（网络主体）

数量：设置 10\~20 个节点（新手建议 15 个），互相连接形成任务协作网络（比如分工完成一项综合任务）。

功能：正常传递任务、数据；节点 / 链路被攻击后失效，无法通信。

攻击智能体（进攻方）

行为：具备学习能力，每轮攻击后总结经验，优先选择网络关键链路 / 核心节点破坏，动态调整攻击策略。

防御智能体（防守方）

行为：实时监测网络状态，发现失效链路 / 节点后进行修复、补充新连接，优化网络拓扑提升抗攻击能力，同样可迭代策略。

3\. 博弈规则（简单自洽，新手好落地）

初始状态：网络拓扑固定，所有节点、链路正常工作，网络可完整完成任务。

轮次制对抗：一轮 = 攻击动作 → 网络状态更新 → 防御动作 → 网络状态更新。

终止条件：连续 N 轮网络彻底瘫痪（任务无法流转），攻击方胜利；达到预设最大轮次网络仍可运行，防御方胜利。

智能体能力限制：每一轮攻击方最多切断 2 条链路，防御方最多修复 2 条链路 / 新增 1 条链路（避免博弈无边界）。

三、分步落地实现（小白一步步做，共 7 大步骤）

前置准备：环境搭建（10 分钟完成）

步骤 1：安装基础依赖

打开电脑命令提示符 / 终端，逐行执行以下安装命令：

bash

运行

\# 核心库：网络建模、可视化

pip install networkx matplotlib plotly

\# 多智能体框架 AgentScope

pip install agentscope

\# 模型调用工具（可选，对接大模型用）

pip install ollama requests

步骤 2：选择智能体模式（三选一，按自身能力选）

难度从低到高：纯规则智能体（推荐新手）> 本地开源大模型智能体 > 在线 API 智能体

入门版（纯规则，无大模型）：只用 Python 条件判断写攻防策略，零模型部署，优先选这个完成作业。

进阶版（本地大模型）：用 Ollama 部署 Qwen，让智能体自然语言决策、学习经验。

快速版（在线 API）：申请免费大模型 API，代码简单，需要网络。

阶段 1：搭建基础任务网络（核心第一步，15 分钟）

目标：用 NetworkX 创建初始协作网络，定义节点、链路、任务流转逻辑。

新建 Python 文件 build\_network.py

编写代码创建网络（示例：15 个任务节点，随机连通网络）

基础功能实现：

计算节点度中心性、介数中心性（识别网络关键节点 / 链路）

编写函数：切断链路、恢复链路、判断网络是否连通（网络瘫痪判定标准）

可视化初始网络：用 Matplotlib 画出节点和连线，标记核心节点。

核心判断逻辑：网络连通性 = 鲁棒性基础，网络不连通则任务失败。

阶段 2：设计三类智能体（分版本实现）

方案 A：纯规则智能体（零基础首选，重点讲解）

无需大模型，用固定策略 + 简单 “学习迭代” 模拟自适应能力，完全满足作业要求。

任务节点智能体

仅保留基础属性：节点 ID、是否存活、相邻节点，负责任务传递，无复杂决策。

攻击智能体（带简单学习）

初始策略：随机选择链路攻击（模拟传统随机攻击）。

学习规则（自适应升级）：

① 记录每一轮攻击效果：攻击哪条链路后，网络连通度下降最明显；

② 后续轮次优先攻击历史高效果链路（模拟智能体学习、自适应攻击）；

③ 避开多次攻击无效的链路。

防御智能体（带简单学习）

监测网络：实时扫描失效链路、高风险核心节点；

修复策略：优先修复被频繁攻击的关键链路；

拓扑优化：若某节点反复被攻击，主动新增旁路链路，分散风险。

方案 B：大模型驱动智能体（进阶，追求创新分）

基于 AgentScope + Ollama 本地模型，让智能体用自然语言思考、制定攻防策略：

用 Ollama 一键部署通义千问 7B：ollama run qwen

在 AgentScope 中定义 3 类智能体角色，配置角色提示词：

攻击智能体提示词：你是网络攻击智能体，根据当前网络状态和历史攻击记录，选择最优链路进行破坏，每轮最多攻击2条链路。

防御智能体提示词：你是网络防御智能体，监测网络受损情况，优先修复关键链路，优化网络结构抵御攻击。

开启智能体记忆功能：保存每一轮博弈日志，实现 “互相学习” 的猫鼠游戏。

阶段 3：编写动态博弈主流程（实验核心，20 分钟）

新建主程序 main\_game.py，实现轮次攻防循环，流程固定：

初始化：加载初始网络、启动所有智能体、清空博弈日志。

循环执行（设置最大博弈轮次，比如 30 轮）：

攻击阶段：攻击智能体根据策略选择链路 → 切断链路 → 记录攻击行为与网络状态。

状态检测：计算当前网络各项指标，判断是否已经瘫痪。

防御阶段：防御智能体执行修复 / 增链操作 → 更新网络。

智能体学习：攻防双方更新自身策略（复盘上一轮结果）。

数据存档：保存本轮所有数据，用于后续分析。

循环结束：输出博弈结果（攻防胜负、总轮次）。

阶段 4：网络鲁棒性指标量化（对应作业考核：涌现现象量化）

作业要求用网络指标刻画攻防演化，新手只需要计算以下 5 个基础指标，每一轮都记录数值：

网络连通性：是否全连通（最核心，判断网络是否失效）

平均最短路径：路径越长，网络传输效率越低，鲁棒性越差

聚类系数：反映网络局部聚集能力，受损后系数会下降

最大连通子图规模：网络被攻击后，剩余正常节点的数量（鲁棒性核心指标）

关键节点中心性变化：核心节点被攻击后，中心性波动情况

每一轮博弈都把指标存入表格，后续画图分析变化趋势。

阶段 5：可视化与动态演示（提升展示得分）

静态图表：用 Matplotlib 绘制折线图，展示30 轮博弈中各项鲁棒性指标的变化曲线。

动态网络演示：

逐轮刷新网络拓扑，用不同颜色标记：正常链路（黑色）、被攻击链路（红色）、修复链路（绿色）。

可制作 GIF / 短视频，直观展示攻防全过程（作业提交视频素材）。

博弈日志：输出每一轮攻防行为、决策内容、网络状态，方便复盘。

阶段 6：对照实验（突出选题创新点，加分项）

本课题核心是对比传统攻击 VS AI 智能体自适应攻击，必须做对照组实验：

对照组 1：传统随机攻击

攻击方完全随机切断链路，无学习、无自适应，运行 30 轮，记录指标。

对照组 2：传统蓄意攻击（按节点度数攻击）

攻击方固定选择度数最高的节点 / 链路攻击，策略不变。

实验组：AI 自适应攻防博弈

本次实验主体，攻防智能体持续学习、动态变策略。

横向对比三组实验的鲁棒性指标，证明：智能体自适应攻击对网络破坏更强，传统静态鲁棒性分析不再适用，扣回课题背景。

