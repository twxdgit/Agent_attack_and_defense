# 多智能体任务博弈的动态攻防网络与鲁棒性对抗

## 实验设计方案 V2.0

---

## 目录

1. [项目概述与目标](#一项目概述与目标)
2. [系统架构设计](#二系统架构设计)
3. [Web界面设计](#三web界面设计)
4. [后端API设计](#四后端api设计)
5. [前端技术实现](#五前端技术实现)
6. [核心功能模块](#六核心功能模块)
7. [网络鲁棒性指标体系](#七网络鲁棒性指标体系)
8. [项目目录结构](#八项目目录结构)
9. [部署与运行](#九部署与运行)

---

## 一、项目概述与目标

### 1.1 项目简介

本项目设计并实现一个**多智能体攻防博弈可视化平台**，通过图形化Web界面实时展示网络攻防博弈的全过程。用户可以配置实验参数、观察智能体行为、查看网络结构变化，并分析博弈结果。

### 1.2 核心功能目标

| 功能模块 | 描述 |
|---------|------|
| 参数配置 | 设置博弈轮次、网络类型、大模型使用情况等 |
| 智能体监控 | 实时显示攻击/防御/任务节点智能体状态和任务进度 |
| 网络可视化 | 实时展示网络拓扑结构变化（节点、链路状态） |
| 指标分析 | 博弈结束后绘制指标图表，分析智能体学习效果 |
| 实验日志 | 记录并展示完整博弈过程日志 |

### 1.3 技术选型

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端框架 | Vue 3 + TypeScript | 现代化响应式UI框架 |
| 状态管理 | Pinia | Vue官方推荐状态管理 |
| 网络图可视化 | D3.js / vis-network | 交互式网络拓扑展示 |
| 图表库 | ECharts | 指标图表绘制 |
| 后端框架 | Flask + FastAPI | 轻量级Python Web服务 |
| 实时通信 | WebSocket / SSE | 实时推送博弈状态 |
| 原有核心 | NetworkX | 网络建模与指标计算 |

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         Web 浏览器                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │
│  │ 参数配置面板 │  │ 网络拓扑视图 │  │ 智能体状态面板 │               │
│  └─────────────┘  └─────────────┘  └─────────────┘               │
│  ┌─────────────┐  ┌─────────────┐                                │
│  │ 指标图表面板 │  │ 博弈日志面板 │                                │
│  └─────────────┘  └─────────────┘                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTP/WebSocket
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Flask API Server                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │
│  │ 实验配置API │  │ 博弈执行API │  │ 结果分析API │               │
│  └─────────────┘  └─────────────┘  └─────────────┘               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Python 核心引擎                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │
│  │ NetworkManager │ │ GameEngine  │ │ RobustnessMetrics│         │
│  └─────────────┘  └─────────────┘  └─────────────┘               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │
│  │ AttackerAgent│ │ DefenderAgent│ │ TaskNodeAgent│              │
│  └─────────────┘  └─────────────┘  └─────────────┘               │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流设计

```
用户操作 ──▶ 参数配置 ──▶ 启动实验
                            │
                            ▼
                    后端接收请求
                            │
                            ▼
                    初始化博弈引擎
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
        攻击智能体    防御智能体    任务节点智能体
            │               │               │
            └───────────────┼───────────────┘
                            │
                            ▼
                    执行单轮博弈
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
        网络状态      智能体状态      指标数据
            │               │               │
            └───────────────┼───────────────┘
                            │
                            ▼
                    WebSocket推送
                            │
                            ▼
                    前端实时更新
```

---

## 三、Web界面设计

### 3.1 页面布局

```
┌────────────────────────────────────────────────────────────────────────┐
│  🛡️ 网络攻防博弈平台                              [实验历史] [帮助]     │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌─────────────────┐  ┌────────────────────────────────────────────┐  │
│  │   参数配置面板    │  │            网络拓扑可视化                    │  │
│  │                 │  │                                            │  │
│  │ 博弈轮次: [30]   │  │      ┌───┐      ┌───┐                      │  │
│  │ 节点数量: [8]    │  │      │ 0 │──────│ 1 │                      │  │
│  │                 │  │      └─┬─┘      └─┬─┘                      │  │
│  │ 网络类型:        │  │        │    ╳     │                         │  │
│  │ ○ 小世界网络    │  │      ┌─┴─┐      ┌─┴─┐                      │  │
│  │ ● BA无标度网络  │  │      │ 2 │──────│ 3 │                      │  │
│  │ ○ 完全图       │  │      └───┘      └───┘                      │  │
│  │                 │  │                                            │  │
│  │ ☑ 使用大模型    │  │  图例: ● 正常  ● 被攻击  ● 已修复  ● 新增    │  │
│  │ 模型: [Qwen]   │  │                                            │  │
│  │                 │  ├────────────────────────────────────────────┤  │
│  │ 攻击限制:       │  │            当前轮次: 5 / 30                  │  │
│  │ 每轮 [2] 条链路 │  │  ━━━━━━━━━━━━━━━━━━━━━━━░░░░░░░░          │  │
│  │                 │  │                                            │  │
│  │ 防御限制:       │  └────────────────────────────────────────────┘  │
│  │ 修复 [2] 条    │                                                │
│  │ 新增 [1] 条    │  ┌────────────────────────────────────────────┐  │
│  │                 │  │              智能体状态面板                 │  │
│  │ ─────────────── │  │                                            │  │
│  │                 │  │  🤖 攻击智能体                              │  │
│  │ [  开始实验  ]  │  │     当前任务: 分析网络关键节点                 │  │
│  │ [  暂停实验  ]  │  │     任务进度: ████████░░ 80%                │  │
│  │ [  停止实验  ]  │  │     已攻击链路: 12条  |  命中率: 75%          │  │
│  │                 │  │                                            │  │
│  │ 速度: [快▼]    │  │  🛡️ 防御智能体                              │  │
│  │ ○ 慢速  ● 正常 │  │     当前任务: 修复受损链路                     │  │
│  │ ○ 快速  ○ 极速 │  │     任务进度: ██████░░░░ 60%                │  │
│  │                 │  │     已修复链路: 8条   |  修复率: 67%         │  │
│  └─────────────────┘  │                                            │  │
│                       │  📋 任务节点智能体 (8个)                     │  │
│                       │     存活: 7/8  |  正常链路: 15条            │  │
│                       └────────────────────────────────────────────┘  │
│                                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                      指标图表面板                                 │  │
│  │  ┌──────────────────┐  ┌──────────────────┐                    │  │
│  │  │ 链路数量变化     │  │ 鲁棒性指数变化    │                    │  │
│  │  │      ╱           │  │      ╱           │                    │  │
│  │  │     ╱            │  │     ╱            │                    │  │
│  │  │    ╱             │  │    ╱             │                    │  │
│  │  └──────────────────┘  └──────────────────┘                    │  │
│  │  ┌──────────────────┐  ┌──────────────────┐                    │  │
│  │  │ 连通分量比例     │  │ 聚类系数变化     │                    │  │
│  │  └──────────────────┘  └──────────────────┘                    │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                      博弈日志                                    │  │
│  │  [10:30:15] 第3轮: 攻击智能体选择攻击链路 (1,3)                  │  │
│  │  [10:30:16] 第3轮: 防御智能体开始修复链路...                     │  │
│  │  [10:30:17] 第3轮: 新增链路 (2,5)                               │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
```

### 3.2 界面功能分区

| 区域 | 功能 | 技术实现 |
|------|------|---------|
| 左侧参数面板 | 实验参数配置、控制按钮 | Vue表单组件 |
| 中央网络视图 | 实时网络拓扑展示 | D3.js / vis-network |
| 右侧状态面板 | 智能体状态监控 | Vue响应式组件 |
| 下方图表区 | 指标变化图表 | ECharts |
| 底部日志区 | 博弈过程日志 | 虚拟滚动列表 |

### 3.3 交互设计

**网络节点交互：**
- 点击节点：显示节点详细信息（度数、介数中心性、状态）
- 悬停链路：显示链路信息（两端节点、状态、攻击/修复历史）
- 拖拽布局：可调整网络布局

**智能体面板交互：**
- 点击智能体卡片：展开详细任务历史
- 进度条动画：实时显示任务执行进度

**控制按钮：**
- 开始实验：禁用参数面板，启用博弈流程
- 暂停/继续：暂停博弈进程，可恢复
- 停止实验：结束当前实验，保存结果
- 重置：恢复初始状态

---

## 四、后端API设计

### 4.1 API端点

#### 实验配置

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/experiments` | 创建新实验 |
| GET | `/api/experiments/{id}` | 获取实验配置 |
| PUT | `/api/experiments/{id}` | 更新实验配置 |

#### 博弈控制

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/experiments/{id}/start` | 启动博弈 |
| POST | `/api/experiments/{id}/pause` | 暂停博弈 |
| POST | `/api/experiments/{id}/resume` | 继续博弈 |
| POST | `/api/experiments/{id}/stop` | 停止博弈 |

#### 实时数据

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/experiments/{id}/state` | 获取当前状态 |
| GET | `/api/experiments/{id}/stream` | SSE实时流 |
| GET | `/api/experiments/{id}/network` | 获取网络拓扑 |
| GET | `/api/experiments/{id}/agents` | 获取智能体状态 |

#### 结果分析

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/experiments/{id}/metrics` | 获取指标数据 |
| GET | `/api/experiments/{id}/analysis` | 获取分析报告 |
| GET | `/api/experiments/{id}/logs` | 获取博弈日志 |

### 4.2 数据模型

#### 实验配置

```json
{
  "id": "exp_20260615_001",
  "config": {
    "rounds": 30,
    "nodes": 8,
    "network_type": "watts_strogatz",
    "use_llm": true,
    "model": "qwen2.5:7b",
    "max_attacks": 2,
    "max_repairs": 2,
    "max_new_edges": 1,
    "paralysis_threshold": 0.3
  },
  "status": "running"
}
```

#### 博弈状态

```json
{
  "round": 5,
  "max_rounds": 30,
  "network": {
    "nodes": [
      {"id": 0, "status": "active", "degree": 3},
      {"id": 1, "status": "active", "degree": 4}
    ],
    "edges": [
      {"source": 0, "target": 1, "status": "normal"},
      {"source": 1, "target": 2, "status": "attacked"}
    ]
  },
  "agents": {
    "attacker": {
      "current_task": "分析网络关键节点",
      "progress": 0.8,
      "stats": {"attacked": 12, "hit_rate": 0.75}
    },
    "defender": {
      "current_task": "修复受损链路",
      "progress": 0.6,
      "stats": {"repaired": 8, "repair_rate": 0.67}
    },
    "task_nodes": {
      "total": 8,
      "active": 7,
      "normal_edges": 15
    }
  },
  "metrics": {
    "num_edges": 15,
    "largest_cc_ratio": 0.85,
    "robustness_index": 0.72,
    "clustering_coeff": 0.45
  }
}
```

#### 指标数据

```json
{
  "rounds": [0, 1, 2, 3, 4, 5],
  "metrics": {
    "num_edges": [20, 19, 18, 17, 16, 15],
    "largest_cc_ratio": [1.0, 0.95, 0.90, 0.88, 0.86, 0.85],
    "robustness_index": [1.0, 0.88, 0.80, 0.76, 0.74, 0.72],
    "clustering_coeff": [0.5, 0.48, 0.46, 0.45, 0.45, 0.45]
  }
}
```

### 4.3 实时通信（SSE）

```python
@app.route('/api/experiments/<exp_id>/stream')
def stream_experiment(exp_id):
    def generate():
        while True:
            state = get_current_state(exp_id)
            yield f"data: {json.dumps(state)}\n\n"
            time.sleep(0.5)  # 500ms刷新
    return Response(generate(), mimetype='text/event-stream')
```

---

## 五、前端技术实现

### 5.1 项目结构

```
frontend/
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── api/
│   │   └── experiment.ts          # API调用
│   ├── components/
│   │   ├── ConfigPanel.vue         # 参数配置面板
│   │   ├── NetworkGraph.vue        # 网络拓扑图
│   │   ├── AgentStatus.vue         # 智能体状态
│   │   ├── MetricsChart.vue       # 指标图表
│   │   └── GameLog.vue             # 博弈日志
│   ├── stores/
│   │   └── experiment.ts           # Pinia状态管理
│   ├── types/
│   │   └── index.ts               # TypeScript类型定义
│   └── utils/
│       └── format.ts               # 工具函数
├── public/
├── index.html
├── vite.config.ts
└── package.json
```

### 5.2 核心组件

#### NetworkGraph.vue

```vue
<template>
  <div ref="container" class="network-container"></div>
</template>

<script setup lang="ts">
import { onMounted, watch, ref } from 'vue'
import * as vis from 'vis-network'

const props = defineProps<{
  nodes: NetworkNode[]
  edges: NetworkEdge[]
}>()

const container = ref<HTMLElement>()
let network: vis.Network | null = null

onMounted(() => {
  const data = {
    nodes: props.nodes.map(n => ({
      id: n.id,
      label: String(n.id),
      color: getNodeColor(n.status)
    })),
    edges: props.edges.map(e => ({
      from: e.source,
      to: e.target,
      color: getEdgeColor(e.status)
    }))
  }
  
  network = new vis.Network(container.value!, data, options)
})

function getNodeColor(status: string) {
  const colors = {
    active: '#4CAF50',
    attacked: '#F44336',
    repaired: '#2196F3'
  }
  return colors[status] || '#999'
}
</script>
```

#### MetricsChart.vue

```vue
<template>
  <div ref="chartContainer" class="metrics-chart"></div>
</template>

<script setup lang="ts">
import * as echarts from 'echarts'

const props = defineProps<{
  metrics: MetricsData
}>()

const chartContainer = ref<HTMLElement>()
let chart: echarts.ECharts | null = null

onMounted(() => {
  chart = echarts.init(chartContainer.value!)
  chart.setOption({
    title: { text: '网络鲁棒性指标变化', textStyle: { fontSize: 14 } },
    tooltip: { trigger: 'axis' },
    legend: { data: ['链路数量', '鲁棒性指数'] },
    xAxis: { type: 'category', data: props.metrics.rounds },
    yAxis: [
      { type: 'value', name: '链路数量' },
      { type: 'value', name: '鲁棒性指数', min: 0, max: 1 }
    ],
    series: [
      { name: '链路数量', type: 'line', data: props.metrics.num_edges },
      { name: '鲁棒性指数', type: 'line', yAxisIndex: 1, data: props.metrics.robustness_index }
    ]
  })
})
</script>
```

### 5.3 状态管理 (Pinia)

```typescript
// stores/experiment.ts
import { defineStore } from 'pinia'

export const useExperimentStore = defineStore('experiment', {
  state: () => ({
    experimentId: null as string | null,
    status: 'idle' as 'idle' | 'running' | 'paused' | 'finished',
    config: null as ExperimentConfig | null,
    currentRound: 0,
    network: null as NetworkState | null,
    agents: null as AgentsState | null,
    metrics: [] as RoundMetrics[],
    logs: [] as GameLog[]
  }),
  
  actions: {
    async startExperiment(config: ExperimentConfig) {
      const response = await api.createExperiment(config)
      this.experimentId = response.id
      this.status = 'running'
      this.config = config
      this.connectStream()
    },
    
    connectStream() {
      const eventSource = new EventSource(`/api/experiments/${this.experimentId}/stream`)
      eventSource.onmessage = (event) => {
        const state = JSON.parse(event.data)
        this.updateState(state)
      }
    }
  }
})
```

---

## 六、核心功能模块

### 6.1 实验配置模块

| 功能 | 描述 |
|------|------|
| 参数表单 | 博弈轮次、节点数量、网络类型等 |
| 参数校验 | 范围验证、依赖检查 |
| 配置预览 | 显示完整配置摘要 |
| 保存/加载 | 支持配置模板 |

### 6.2 博弈执行模块

| 功能 | 描述 |
|------|------|
| 单步执行 | 执行单个博弈轮次 |
| 连续执行 | 按设定速度连续执行 |
| 暂停/继续 | 中断后可恢复 |
| 速度控制 | 慢速/正常/快速/极速 |

### 6.3 网络可视化模块

| 功能 | 描述 |
|------|------|
| 节点渲染 | 状态着色、大小映射 |
| 链路渲染 | 颜色区分、动态效果 |
| 交互操作 | 点击、悬停、拖拽 |
| 布局算法 | 力导向图布局 |

### 6.4 智能体状态模块

| 功能 | 描述 |
|------|------|
| 状态展示 | 当前任务、进度 |
| 统计信息 | 攻击/修复统计 |
| 历史记录 | 任务执行历史 |
| 学习分析 | 策略演进分析 |

### 6.5 指标分析模块

| 功能 | 描述 |
|------|------|
| 实时图表 | 博弈过程中的指标变化 |
| 汇总报告 | 实验结束后生成报告 |
| 对比分析 | 多实验结果对比 |
| 导出功能 | 导出图表、数据 |

---

## 七、网络鲁棒性指标体系

### 7.1 指标定义

| 指标 | 公式/定义 | 说明 |
|------|----------|------|
| 链路数量 | $E_{current}$ | 网络中剩余链路数 |
| 最大连通分量比例 | $\frac{|CC_{max}|}{N}$ | 最大连通分量节点数/总节点数 |
| 聚类系数 | $C = \frac{1}{N}\sum_i \frac{2E_i}{k_i(k_i-1)}$ | 节点邻居间的连接程度 |
| 鲁棒性指数 | $R = \frac{1}{N}\sum_i \frac{N_i}{i}$ | 网络抗攻击能力 |
| 平均路径长度 | $L = \frac{1}{N(N-1)}\sum_{i\neq j} d_{ij}$ | 信息传输效率 |

### 7.2 智能体学习效果评估

**攻击智能体评估：**
- 攻击效率：破坏链路数 / 攻击次数
- 关键链路识别：攻击高介数中心性链路比例
- 学习收敛：攻击命中率随轮次变化

**防御智能体评估：**
- 修复效率：修复链路数 / 修复次数
- 网络加固：新增链路对鲁棒性的提升
- 资源分配：优先修复关键链路的准确率

---

## 八、项目目录结构

```
Agent_attack_and_defense/
├── backend/                      # 后端服务
│   ├── __init__.py
│   ├── app.py                    # Flask应用入口
│   ├── api/
│   │   ├── __init__.py
│   │   ├── experiments.py        # 实验API
│   │   └── results.py            # 结果API
│   └── services/
│       ├── __init__.py
│       └── game_runner.py        # 博弈执行器
│
├── frontend/                     # Vue前端
│   ├── public/
│   ├── src/
│   │   ├── api/
│   │   ├── components/
│   │   ├── stores/
│   │   ├── types/
│   │   ├── utils/
│   │   ├── App.vue
│   │   └── main.ts
│   ├── index.html
│   ├── package.json
│   └── vite.config.ts
│
├── core/                         # 核心引擎（原有）
│   ├── game_engine.py
│   ├── network_manager.py
│   └── model_manager.py
│
├── agents/                       # 智能体（原有）
│   ├── attack_agent.py
│   ├── defense_agent.py
│   └── task_node_agent.py
│
├── metrics/                      # 指标计算（原有）
│   ├── robustness.py
│   └── visualizer.py
│
├── data/                         # 数据目录
│   ├── logs/                     # 实验日志
│   └── results/                  # 实验结果
│
├── requirements.txt
├── README.md
├── 设计方案.md
└── 设计方案V2.0.md
```

---

## 九、部署与运行

### 9.1 环境要求

- Python 3.8+
- Node.js 16+
- npm 或 yarn

### 9.2 后端启动

```bash
# 安装Python依赖
pip install -r requirements.txt

# 启动后端服务
python backend/app.py
# 服务运行在 http://localhost:5000
```

### 9.3 前端启动

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
# 服务运行在 http://localhost:3000
```

### 9.4 访问平台

打开浏览器访问 `http://localhost:3000`

---

## 十、实现计划

| 阶段 | 任务 | 时间 |
|------|------|------|
| 1 | 搭建前端项目结构 | 1天 |
| 2 | 实现后端API | 1天 |
| 3 | 网络拓扑可视化组件 | 1天 |
| 4 | 智能体状态面板 | 1天 |
| 5 | 指标图表组件 | 1天 |
| 6 | 实时通信集成 | 1天 |
| 7 | 样式优化与测试 | 1天 |

---

*文档版本: V2.0*
*更新日期: 2026-06-15*
